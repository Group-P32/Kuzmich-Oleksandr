@model JewelryStore.Models.Jewelry
@{
    ViewBag.Title = "Редагування прикраси";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h2>Редагування прикраси</h2>

@using (Html.BeginForm())
{
    @Html.AntiForgeryToken()
    <fieldset>
        <legend>Прикраса</legend>

        @Html.HiddenFor(m => m.Id)

        <p>
            Назва прикраси<br />
            @Html.EditorFor(m => m.Name)
            @Html.ValidationMessageFor(m => m.Name)
        </p>
        <p>
            Матеріал<br />
            @Html.EditorFor(m => m.Material)
            @Html.ValidationMessageFor(m => m.Material)
        </p>
        <p>
            Ціна (грн)<br />
            @Html.EditorFor(m => m.Price)
        </p>
        <p>
            Категорія<br />
            @Html.DropDownList("CategoryId", ViewBag.CategoryId as SelectList)
        </p>
        <p>
            <input type="submit" value="Зберегти" />
        </p>
    </fieldset>
}

<div>
    @Html.ActionLink("Повернутися до каталогу", "Index")
</div>